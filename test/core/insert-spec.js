import * as tree from '../misc/helpers';

describe('insert', function() {

  it('basic insert', function(done) {
    tree.equals(this, done);
  });

  it('insert into select', function(done) {
    tree.equals(this, done);
  });

  it('insert into default', function(done) {
    tree.equals(this, done);
  });

  it('insert on conflict', function (done) {
    tree.equals(this, done);
  });

  it('insert returning', function (done) {
    tree.equals(this, done);
  });
});
